export function ExercisesScreen() {
  return (
    <div className="screen">
      <h1>Exercises</h1>
      <p>
        Built-in exercises (from <code>exercises.xml</code>) will be bundled as
        JSON in a later iteration. Use the desktop build or import a project
        for now.
      </p>
    </div>
  );
}
